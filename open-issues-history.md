# Open Issues — superseded history (Rachel, AI Finance Assistant)

_Where a live `RA-<n>` row in `open-issues.md` has grown long enough to threaten Drive's ~31 KB silent-truncation
point (`HL-0005`, measured at **31,316 bytes**), its **superseded narrative** is moved here **verbatim** and the live
row keeps the current position plus a pointer. **Nothing is edited, summarised or deleted** — the reasoning, including
the wrong turns, is the point of keeping it._

_This is the third file in the set: `open-issues.md` (live, open), `open-issues-resolved.md` (resolved in full),
`open-issues-history.md` (superseded narrative of still-open issues)._

_**Mirrored to git since 2026-09-19** (Minda). `CHARTER.md` §2's mirror list was extended from four files to **seven**
so the superseded record is versioned alongside the live one. Drive is still the residence; git is still only the mirror._

_Opened 2026-09-19, when the live table reached 30,111 bytes with no resolved rows left to move out — the first time
the **open** table alone outgrew one file._

## RA-19 — full history as at 2026-09-19
Moved here because the live row had reached **5,144 characters**, the largest in the log, carrying five dated
appendices. The live row in `open-issues.md` now states the current position; everything below is the row as it
stood, verbatim.

| RA-19 | 2026-09-19 | Open | **Consolidate the scattered financial documents into the designated main Financial Archive.** With the home now decided (`RA-10`, folder `1BVk_RfuJ3rBRujZUMC98KMlil4AkICL4`), the work of actually bringing documents together remains. What sits outside it today: the **Fishbone Group KB `Archive/`** (Construction FY2023 + FY2024 + CT600, Holdings FY2024/25, Waste FY2024/25, Properties FY2024/25 + CT600, Commercial FY2025 + CT600, plus incorporation, change-of-name, VAT and UTR certificates); the **OneDrive tax archive SRC-32** (16 statutory accounts sets FY2015→FY2023, `Mortgage docs/`, `HMRC/`, the `Tax 20xx-xx/` folders); and finance material in the **six sister KBs**. **Rachel cannot do this herself** — `CHARTER.md` §3 forbids her moving or editing anything inside another KB or the Financial Archive's source files, and is silent on filing into it (`RA-20`). So this needs either Minda's own hands, an extension of Rachel's reach, or a routed task. Sequence it **after index v2** (`RA-17`), which will list precisely what to move and from where. **2026-09-19:** Collaboration Space and OneDrive are now **ruled out as destinations** for financial documents (owner ruling), so the archive is the only target. The authority Rachel would need is set out in `RA-20`, and her recommended method is **copy, byte-verify, register, then retire** — never a straight move, and no deletion by Rachel. One knock-on to plan for: the group KB and sister KBs **cite OneDrive paths and Drive file ids** as sources (`SRC-32` and others). Moving a file within Drive keeps its id, but copying from OneDrive creates a new id, so those citations will need updating — a cross-employee job for Victoria or Alex, not something Rachel edits in another KB. **2026-09-19 — UNBLOCKED.** All three `RA-20` authorities granted, so the consolidation is Rachel's to do: copy in, byte-verify, register, retire. **Still sequenced after index v2** (`RA-17`) — that is Rachel's own discipline, not a limit Minda set: moving several hundred documents before knowing exactly which are duplicates of which would create the mess consolidation is meant to end. Order of work: index v2 → a per-document move list with source and destination → consolidate in batches, byte-verified and registered as it goes → retire sources last, once the archive copy is proven. Peter's `AWT-0016`/`AWT-0017` documents now have a landing place and do not have to wait for any of it. **2026-09-19 — move list drafted** (index v2 §11), in five batches: **(1)** the **dated FY2024 set** for Properties, Holdings and Waste plus the dated FY2023 pair for Construction — these exist **only** in the group KB `Archive/` and nowhere else, so they are the highest-value batch; **(2)** the tax returns (FC CT600 2024, FP CT600 2025, and the CP pair **after** `RA-27` is settled); **(3)** the loan packs (Landbay 70092357's ten files, the MotoNovo pack, one of three SSAS Loanback copies); **(4)** incorporation, VAT and UTR certificates, one copy each, **credentials excluded**; **(5)** the bank-statement series, once a folder convention is agreed. **Explicitly not in any batch:** the Government Gateway credentials (`RA-25`), the SSAS pension records for named individuals, the payroll reports, the tenant identity documents, and anything still unidentified (`RA-28`). **2026-09-19 — BLOCKED, see `RA-30`.** Batches 1–5 do not proceed until the group's locked document policy is versioned: v1.3 §7 files documents in the Collaboration Space and §10 bars sister-KB moves, both contrary to Minda's rulings. Running the batches anyway would fork a group rule the other six KBs still follow. **2026-09-19 — UNBLOCKED by owner ruling (`RA-30`), and BATCH 1 IS DONE.** Five documents relocated into the Financial Archive, each into its company's existing `Annual Accounts` subfolder rather than the archive root, because those folders exist and filing into them is adding, not restructuring: **FP** Dated Members Accounts 2024 (1,095,647 → `1teAPIEBu-LoM6KL_3shWg5FfQ6XNxQdd`); **FH** Dated Members Accounts 2024 (907,868 → `1mgauYRehiziqpdEZY7s5HpcBU6FVog0Q`); **FW** Dated Members Accounts 2024 (905,793 → `18wcIBdb-2uXouMmS1UL2YL8voRHIzmSn`); **FC** Dated Members Accounts and Dated Pages for Registrar YE 29 Apr 2023 (126,890 and 100,986 → `19k8n3NwR5V4JICqQ-zYC0VwrzbH0sooc`). Same-Drive moves, so **every file id is unchanged** and byte sizes are unchanged; re-queried afterwards and the group KB `Archive/` now holds **no** dated statutory document. One byte-identical duplicate retired: Properties' own KB `Raw/Finance` copy (`1pXt93bGxM7joY0WQcl8T7R6UGpKh-8hv`) moved to Rachel's `Archive/` and labelled with the primary's id — moved, not deleted. **Why Batch 1 first:** these five were the only copies in existence for the dated FY2024 set (FH, FW) and the dated FY2023 pair (FC), so they carried the most risk sitting in a governance archive. **Still ahead:** Batch 2 waits on `RA-27`; Batches 3–5 are clear to run; registration of everything moved so far waits only on Rachel assigning IDs under the policy's scheme. |

## Second move, 2026-09-19 — seven rows

_Moved when `open-issues.md` reached **29,768 bytes**, 1,548 short of the measured 31,316 truncation point, with all 20 rows
Open and nothing left to retire. Each row below is **the row as it stood, verbatim**; the live row in `open-issues.md` now states
the current position and points here. Nothing is edited, summarised or deleted._

### RA-2 — full history as at 2026-09-19
Peter's delivery closed the hunt, so the whole search narrative — Victoria's original finding, index v1's OneDrive discovery for Drylining, the routing to `AWT-0016` and the Org-profile refinement — is now history. The live row states what is left: two named Hub tasks.

| RA-2 | 2026-09-18 | Open | **FY2023 statutory-accounts gap.** Found 2026-09-18 (Victoria): the group holds FY2024 + FY2025 statutory accounts for Properties, Holdings and Waste, but **FY2023 (year-end April 2023) is missing** for all three — not in the KB and not in the Finance archive (whose "Annual Accounts" folders hold only FY2025; older year-folders are bank/payroll, not accounts). Construction is complete (FY2023–25); Commercial Properties' whole life is covered (periods to 31/10/2023 and 30/04/2025). The three FY2023 sets are **public and filed at Companies House** — obtain and fold into the finance archive. **2026-09-18 (Rachel, index v1):** FY2023 accounts **found on OneDrive for Fishbone Drylining Ltd** in two places (`Fishbone Tax/Annual Accounts/JAN-24-2024 Dated Members Accounts Ye 29 April 2023 - Fishbone Drylining.pdf` and `HMRC/050224/FISHBONE_DRYLINING_LTD. - Accounts.pdf`). Still **not found in either cloud** for Properties, Holdings or Waste. **2026-09-19 (Rachel):** routed to Peter as **`AWT-0016`** in the Hub `Tasks & Requests` (Open, High, due 2026-09-26), per Minda's ruling that external-register retrieval goes through Peter. RA-2 stays open until the three sets land and are registered. **2026-09-19 (Rachel, after reading the group KB Org profiles):** company numbers added to `AWT-0016` (Properties 09687012, Holdings 10146262, Waste 13201875) and the already-held years listed so Peter does not duplicate them. Construction is confirmed complete — its FY2023 accounts as Fishbone Drylining Ltd are in the group KB Archive. The three FY2023 gaps stand. **2026-09-19 — Peter delivered (`AWT-0016`), checked, and it is right but not finished.** All three FY2023 sets retrieved from Companies House via document-api, made up to 30 Apr 2023, with barcodes, page counts and sha256, and registered: **FP0000140**, **FW0000003**, and Holdings as FH0000001 — **which is a duplicate number and must become FH0000020** (the FH sequence already runs to FH0000019; breaks §3 “numbers are never reused” and §4's one-ID invariant). Routed back to Peter; Rachel did not edit his row (§10). **RA-2 STAYS OPEN because the PDFs are in no Drive folder** — Location reads `PENDING` on all three register rows. Peter's tooling cannot relay the bytes and Rachel may not fetch from an external register (owner ruling), so the upload falls to Alex or Minda — and Peter had already logged the underlying tooling gap as **`HL-0014`** (no practical way to relay a downloaded binary into Drive), which is the real blocker. **Destination corrected** from the KB `/Raw`/Collaboration Space that v1.3 implies to the Financial Archive `Annual Accounts` subfolders — FP `1teAPIEBu-LoM6KL_3shWg5FfQ6XNxQdd`, FH `1mgauYRehiziqpdEZY7s5HpcBU6FVog0Q`, FW `18wcIBdb-2uXouMmS1UL2YL8voRHIzmSn`. Full note on the `AWT-0016` row comment. **Routed on 2026-09-19 (second pass):** the two loose ends from Peter's delivery now have their own Hub rows rather than sitting in a thread — `AWT-0027` (Peter, High) to renumber `FH0000001` to `FH0000020`, and `AWT-0028` (Alex, High) to complete the physical filing into the Financial Archive, which is still `Location: PENDING` on all three (`HL-0014`). A pointer comment was added to `AWT-0016` as a row discussion, not appended to its Response cell, which is already near the 4,000-character ceiling (`HL-0015`). |

### RA-5 — full history as at 2026-09-19
Narrowed three times (five companies, then three, then two). Only the final position is live; the chain of narrowings is history.

| RA-5 | 2026-09-18 | Open | **CT600 2025 held for only two of five companies.** The Finance archive's `Annual Accounts` folders hold a CT600 2025 for **Properties** and **Commercial Properties** only. **Construction, Holdings and Waste** have members accounts and registrar pages but **no CT600**. Either the returns were not saved to the archive or they were not prepared — establish which, with the accountant. **2026-09-19 (Rachel):** folded into **`AWT-0017`** as a secondary check on Peter's filing-history review. CT600s are filed with HMRC rather than Companies House, so if they are not visible this returns to Rachel as an accountant question. **2026-09-19 (Rachel):** narrowed — **Construction's FY2024 CT600 IS held** (£15,747.25 on £62,989), and Commercial Properties' CT600 for 1 Nov 2023–31 Oct 2024 is held (£1,484.47 **unpaid at signing**). The gap is **FY2025 only**, for Construction, Holdings and Waste. See `Archive-Index/finance-document-index-v1-CORRECTIONS-2026-09-19.md`. **2026-09-19 — narrowed again, and the Companies House avenue is now closed.** Peter confirmed (`AWT-0017`) that no tax or CT600 categories appear in CH filing history for Holdings, Construction or Waste: CT600s go to HMRC, so this is a question for **RMT**, not a retrieval. **And a correction Rachel found while checking the register, not from being told:** `FH0000015` is Holdings' **CT600 for the year ended 30 Apr 2025**, held in the Holdings KB `Outputs/Correspondence`. So the FY2025 CT600 gap is **Construction and Waste only** — two companies, not three. |

### RA-11 — full history as at 2026-09-19
The pre-precedence framing (“establish the primary copy for each”) is superseded by the owner's precedence rule, and the duplicate inventory's real home is index v2 §7, which the live row now cites by Drive id rather than restating.

| RA-11 | 2026-09-18 | Open | **Cross-cloud and within-cloud duplicates.** **Added 2026-09-19:** the **entire OneDrive `SSAS/` folder (141 MB) duplicates the Google Drive `SSAS/` folder** — same trust deed, scheme rules, board minutes, administration agreement, Pensions Regulator certificate, EP documents, the same six photographs and the same eight subfolders. The Drive copy is canonical (`SRC-34`, with the SSAS KB as `SRC-33`), so index v2 records the OneDrive set as a duplicate and does not re-catalogue it. Also: the BTL property schedules in `Mortgage docs/` overlap the Drive `Budget/Copy of BTL_Portfolio.xlsx` and the two `BTL Portfolio*.xlsx` files loose in the OneDrive root. `unified_payments.csv` and `20230101-0000_20240430-2359-sales_report.csv` exist in both clouds (Drive: Waste company root; OneDrive: `Fishbone Tax/2023 - 24/`). `FBW budget 2023-24 rev0.xls` exists three times (Drive root `Budget/`, OneDrive `Fishbone Budgeting/`, OneDrive `Fishbone Reports/`) plus an AutoRecovered copy. Several statutory accounts PDFs are duplicated between `Fishbone Tax/Annual Accounts/` and `Mortgage docs/`. The SSAS pension records exist in parallel sets on both clouds. Establish the primary copy for each before anything cites them. **2026-09-19 — precedence rule now set (owner ruling):** where a document exists in more than one place, the copy in the **main Financial Archive** (`1BVk_RfuJ3rBRujZUMC98KMlil4AkICL4`, SRC-31) is **primary**; copies in the Fishbone Group KB `Archive/`, the OneDrive tax archive (SRC-32) and the sister KBs are **secondary**. Index v2 cites the Financial Archive copy and records the others as duplicates. **2026-09-19 — index v2 delivers a byte-level duplicate register** (§7 of `finance-document-index-v2.md`, Drive `14HbMMzhVcEZLj06Hcn5EXT0h3MgyhOWP`). Nine sets are **byte-identical** and safe to retire under the precedence rule: FW Members Accounts 2025 and FW Pages for Registrar 2025 (both duplicated **inside** the group KB `Archive/`), FP Members Accounts 2024 dated, the 145 High Street East pack, the SSAS Loanback LA01801 (**three** copies), the SSAS Legal Mortgage, Completion Statement and Receipted Invoice, and — the largest find — **the entire 24-file incorporation folder, duplicated between the group KB `Archive/` and the Commercial Properties KB `Raw/`**. One set is **not** a duplicate and must not be treated as one: `RA-27`. **2026-09-19 — a tenth duplicate, found through the Document Register rather than a folder walk.** Register row `FH0000001` records Holdings' FYE 2025 annual report at **241,121 bytes** in the Holdings KB `Outputs/Correspondence` (Drive id `1cr5GEDH6xnt6e6i7oC2-0ekw0K-sbMo5`) — **byte-identical** to the group KB `Archive/` copy index v2 already logged (`1v1A3VSvH8ztGuJ4gA7DShHPqn1j_3s7k`). That it surfaced from a register query and not from walking Drive is the argument now made in `RA-15`. **Routed on 2026-09-19:** `AWT-0031` (Alex, Medium) — the remaining byte-identical sets are not finance documents and sit in other employees' knowledge bases, which makes them housekeeping. The ask is the same method Rachel used: decide the primary, then move and label the rest, never delete. The full duplicate register is cited by Drive id so nothing has to be rebuilt. |

### RA-14 — full history as at 2026-09-19
Opened as “no finance documents in either cloud”, which was wrong as written and was restated twice. The live row carries only the confirmed position.

| RA-14 | 2026-09-18 | Open | **AMFA Furniture Ltd has no finance documents in either cloud.** The company has a KB but no accounts, bank statements, budget or tax records were found on Google Drive or OneDrive. Establish whether it trades, and where its books live. **2026-09-19 (Rachel) — restated, partly wrong as first written.** It has none under **Amfa Furniture / Furniture by Fishbone**, which is what index v1 searched. Under its original name **Fishbone Investment Ltd** the OneDrive archive holds Report and Accounts 2018-19, Statutory Accounts 2020-21 and the 2020-21 CT600 — which index v1 did find but filed under a separate “unknown entity”. It was a non-trading shell then. The group KB carries the same gap as its own open question, so this is a known estate issue. Remaining: where the accounts under the current name are. **2026-09-19 (index v2) — confirmed at KB level, and a miss corrected.** `AMFA Furniture Ltd - Knowledge Base` (`1ugshCjwx2yvRXZvmtpwLcg3kUgTKN7aU`) **exists** — v1, the corrections note and `RA-17` all overlooked it, each saying “six sister KBs” when there are seven. Its `Raw/` holds three items and none is an account: `2026-01-27_BP scheme.pdf` (46,605 bytes, unidentified — not opened), one photograph, one owner-note on a furniture catalogue plan. So the gap is real and now confirmed from two directions. **Routed on 2026-09-19:** `AWT-0032` (Alex, Low) — not to re-check Rachel's own three documents, which are already corrected, but to see whether the same “six sister KBs” undercount sits in the estate's **shared** records (Roster, SRC register, housekeeping baselines). An overlooked knowledge base is an unswept one. |

### RA-21 — full history as at 2026-09-19
Opened as “a decision is needed”, then became actionable, then was routed to Victoria. The deliberation is history; the live row is the security statement and the routing.

| RA-21 | 2026-09-19 | Open | **Financial documents already sit in the Collaboration Space, which is domain-writable.** Minda ruled 2026-09-19 that financial documents must not go into Collaboration Space because it is shared. The rule is now in `CHARTER.md` §1 and §3 — but it is forward-looking, and material is **already in there**. The group KB's company profiles record finance subfolders inside Collaboration Space: **`FC Finance`** and `FC Company Documents/Finance and VAT` (Construction), **`FW Finance`** and `FW Bills` (Waste), **`CP Finance`** (Commercial Properties), **`F Finance`** (Furniture/Amfa), plus `FP00 Company` (Properties). And per `AWT-0014`, the Collaboration Space (`1YNj5BIpKVzcmI4U1DRkgu7kcnSDizGEi`) is shared to the **whole `fishboneconstruction.co.uk` domain as writer** — so anything in those folders is readable **and writable** by every domain account. Needed: a decision on whether that existing material is reviewed and relocated to the Financial Archive, and on what timescale. Rachel has **not** opened those folders. **2026-09-19 — now actionable, with one caveat.** With the `RA-20` authorities granted, copying that material into the Financial Archive and registering it is within Rachel's reach and follows directly from Minda's own ruling. The caveat is the **retire** step: those Collaboration Space folders are cited by the sister KBs and used by other AI employees day to day, so removing a copy could break someone else's filing. Rachel will **copy in and register first**, and route the retirement of the Collaboration Space copies through **Victoria** so the citations move with them. Still flagged as a **security** matter, not a filing tidy-up — until those copies go, finance material remains writable by every account on the domain. **2026-09-19:** note the group policy v1.3 §7 positively *requires* Collaboration Space filing, which is the head-on conflict raised as `RA-30` — **resolved by the owner's precedence ruling**, so this relocation is now unambiguously the right course. The Document Register confirms live examples: three FH rows file to `Collaboration Space > FH Company Documents`. **Routed on 2026-09-19:** `AWT-0029` (Victoria, High) — with two questions to settle before anything is retired: whether any copy in the Collaboration Space is the **only** copy, in which case it must be moved to the Financial Archive rather than retired; and whether anyone outside the estate is actively working from them. Rachel has still touched nothing there. |

### RA-22 — full history as at 2026-09-19
Half of this issue resolved itself when the charter caught up with the grant. Keeping the full original framing live would overstate what is still open.

| RA-22 | 2026-09-19 | Open | **The Microsoft 365 connector's grant is wider than the charter allows.** `get_granted_scopes` on 2026-09-19 returned, among others, **`Files.ReadWrite.All`**, **`Mail.Send`**, `Mail.ReadWrite`, `MailboxSettings.ReadWrite` and `Calendars.ReadWrite`. The charter says Rachel has **no Gmail and does not send** (§2) and treats OneDrive as read-only — but the connector would technically let her **write and delete files on OneDrive, send mail as the account, and change mailbox settings**. Rachel's restraint is therefore **policy, not a technical limit**, and `current-state.md` described the connector as “read-only”, which was **factually wrong about the grant**; corrected 2026-09-19. Needed: either narrow the connector's scopes to match the charter, or record explicitly that the wider grant is accepted and unused. Same class of finding as `HL-0007`/`HL-0009` (a charter describing capabilities that do not match the actual connector), but inverted — here the capability exceeds the charter rather than falling short. Raised here rather than on Help & Lessons because it is Rachel's own connector; if it turns out the same grant reaches other employees, it moves to the group desk. **2026-09-19 — partly superseded, and worth being exact about it.** Authority (3) of `RA-20` now **does** let Rachel write on OneDrive, to retire a consolidated source, so `Files.ReadWrite.All` is no longer wider than the charter on the file half — the charter caught up with the grant rather than the reverse. What remains over-wide is the **mail** half: `Mail.Send`, `Mail.ReadWrite` and `MailboxSettings.ReadWrite` serve no purpose in Rachel's role and she does not send or read mail. Narrowed ask: **drop the mail scopes**, keep the files scope. Pairs with `RA-23`. |

### RA-24 — full history as at 2026-09-19
The headline claim was **too broad** and the Document Register disproved part of it. That correction is the point of the row now; the original reasoning is history.

| RA-24 | 2026-09-19 | Open | **No written intercompany loan agreements, and corporation tax unpaid.** Found by index v2 in a Commercial Properties owner-note dated 2026-09-07 (`1BblZb0doDVbcIrgiV5ouomBEstk6NZiv`, not authored by Rachel — she read it in the KB `Raw/`). Two statements, both squarely on Rachel's patch. **(a) The intercompany loans are undocumented.** Money moves between the group companies with no written agreement behind it. That bears directly on the Holdings→Properties interest waiver executed 2026-09-07, on related-party disclosure in the statutory accounts, and on whether the balances in the books can be substantiated at all. **(b) Corporation tax is unpaid.** Matches two independent signals already held: Commercial's CT600 for the period to 31 Oct 2024 records £1,484.47 **unpaid at signing** (`RA-5`), and Waste carries a live £3,042.33 business-rates liability with a 21/08/26 liability order. Needed: confirm the current position per company, then decide whether the agreements are drafted (a job for Minda and RMT, not Rachel — §3 forbids her committing the company). Rachel has **not** contacted HMRC or the accountant. **2026-09-19:** the Document Register shows the waiver chain is fully papered (`FH0000008` request, `FH0000009` board minute, `FH0000012` letter of variation, `FH0000013` acceptance, `FH0000016` confirmation of receipt) and that two underlying loan agreements **do** exist — `FH0000010` (£283,000 at 3.8%, 2019-05-01) and `FH0000011` (£20,000 at 6%, 2021-10-15). So “no written intercompany loan agreements” is **too broad as a general statement**: Holdings→Properties is documented. What the owner-note may mean is the *other* intercompany balances. To clarify with Minda rather than assume either way. |

## `open-issues.md`'s header as it stood before 2026-09-20 — the account of the first three splits

_Moved here verbatim on **2026-09-20**. The live file's header had accumulated four split notes and was 2,952 bytes of mostly **history**, which is not what a header is for. It was replaced with one current note plus this pointer. The text below is unedited, including the line reading “Resolved to date”, whose list is the position at the third split and not today's, and including the fourth-split note that was itself in the header when it was moved._

_**One warning about the block below, because it is reproduced verbatim:** it opens with its own `# Open Issues — Rachel` heading, since it was the top of that file. **It is not the live issue log.** The live log is `open-issues.md` in Rachel's KB root. The block runs from the rule below to the end of this file._

---

# Open Issues — Rachel (AI Finance Assistant)

_The live `RA-<n>` table, **open issues only**, in numeric order. A resolved issue is never deleted — it is
moved, in full, to **`open-issues-resolved.md`** on Drive, and the `RA-<n>` numbers are never reused. Where a
still-open row's narrative grows long enough to threaten the truncation point, its **superseded history** moves
verbatim to **`open-issues-history.md`** and the live row keeps the current position plus a pointer. Rachel's
private issue log; cross-employee problems go on the group **Help & Lessons** desk instead._

_**Split 2026-09-19 (Rachel):** this file had reached **31,248 bytes**, past the **~31 KB** point at which Drive's
`create_file` **silently truncates** (`HL-0005`, measured at 31,316) — so the split stopped being housekeeping and
became the only way to write the file safely. Resolved rows moved out verbatim, nothing edited, nothing lost.
Resolved to date: `RA-4`, `RA-9`, `RA-10`, `RA-12`, `RA-16`, `RA-17`, `RA-18`, `RA-20`, `RA-25`, `RA-27`, `RA-30`.
**Second split the same day:** with 20 rows all Open and nothing left to retire, `RA-19`'s five-appendix history
moved to `open-issues-history.md`. **Third split, on Minda's instruction, at 29,768 bytes:** seven rows moved their
full narrative to `open-issues-history.md` verbatim — `RA-2`, `RA-5`, `RA-11`, `RA-14`, `RA-21`, `RA-22`, `RA-24` —
each chosen because its early reasoning had been **superseded**, not merely because it was long. Live table
**25,743 bytes** at that moment, history **24,549**. Nothing edited, summarised or deleted; every moved row is reproduced in full.
**Mirrored to git since 2026-09-19 (Minda):** `CHARTER.md` §2's mirror list went from four files to seven, so
`open-issues-resolved.md` (27,574) and `open-issues-history.md` are now versioned alongside this file. Drive is
still the residence._

_**Fourth split, 2026-09-20 (Rachel), and it needed a new file.** Adding Batch 4 to `RA-19`, the Batch 4 findings to `RA-11`, `RA-15` and `RA-28`, and opening `RA-31` took this file to **36,798 bytes — 5,482 past** the truncation point (`HL-0005`). The usual remedy failed: `open-issues-history.md` had only ~6.8 KB of headroom and could not take it. So **`open-issues-history-2026-09.md`** was opened — **on Drive only**, exactly as `open-issues-resolved.md` was when it was created, because `CHARTER.md` §2's mirror list names **seven specific files** and widening it is Minda's call, not Rachel's. **Flagged for Minda, not assumed.** `RA-11` and `RA-19` were **rewritten** as compact current-position rows and their previous text moved there **in full, verbatim** — the whole cell, not a chosen extract, so no judgement was exercised about which sentence was superseded. `RA-15`'s registration-and-sampling paragraph went with them. Nothing was summarised away, edited or deleted. This is the problem `RA-31` describes, arriving in the same session it was raised._
