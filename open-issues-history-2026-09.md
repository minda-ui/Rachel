# Open Issues — superseded narrative, September 2026 (Rachel, AI Finance Assistant)

_Companion to `open-issues.md`, `open-issues-resolved.md` and `open-issues-history.md`. **Opened 2026-09-20**, because the
existing history file could not take another move: Drive's `create_file` **silently truncates a single write at 31,316 bytes**
(`HL-0005`), `open-issues-history.md` stood at 24,549 with under 7 KB of headroom, and `open-issues.md` had reached **36,798 —
5,482 bytes past the point**, after Batch 4 was added to four rows and `RA-31` was opened. Dating the file is the fix `RA-31`
proposes for the whole set._

_**Residence and mirror status — read this before assuming.** This file lives on **Google Drive**, in Rachel's KB root, and is
**deliberately not in the git mirror**: `CHARTER.md` §2 names **seven specific files**, and widening that list is **Minda's call,
not Rachel's**. It is therefore **flagged for Minda, not assumed** — exactly how `open-issues-resolved.md` began on 2026-09-19
before Minda widened the mirror from four files to seven. If Minda declines, the file stays Drive-only and nothing breaks; if they
agree, the mirror list becomes eight and `RA-31`'s dating scheme can follow._

_**What is in here, and what was done to it: nothing.** Each section below holds a live row's **entire previous cell text**,
verbatim — the whole cell, not a chosen extract. That was deliberate. Extracting "the superseded part" of a 7,000-byte row means
exercising judgement about which sentence is still true, and doing that while over the truncation point is how detail gets lost.
Moving the whole cell and writing a fresh compact row instead leaves the original intact and auditable. **Nothing here has been
summarised, edited, corrected or deleted**, including statements that were later shown to be wrong — those are the most useful
lines in the file, and `RA-11`'s account of index v2 is one of them._

_**Byte figures in the text below are historical facts** — what something measured at the moment it was written — never claims
about a file as it stands now. To know a file's size, read it (`current-state.md`, on that convention)._


---

## `RA-11` — the set-by-set duplicate discovery narrative, as it stood at 2026-09-20

_The live `RA-11` was rewritten the same day as a compact current-position row: the precedence rule, what the batches closed,
what is still open, and the standing lesson about index v2 being a dated snapshot. Everything below is what it replaced. The
value of keeping it whole is the **sequence** — eighteen duplicate sets found in four different ways, three of which the
byte-level duplicate register in index v2 §7 could not have shown, and two errors (Rachel's, then Alex's) that both came from
trusting that register as current._

| ID | Opened | Status | Issue |
|---|---|---|---|
| RA-11 | 2026-09-18 | Open | **Duplicate documents across and within the clouds.** **Precedence rule (owner ruling, 2026-09-19):** where a document exists in more than one place, the copy in the **main Financial Archive** (`1BVk_RfuJ3rBRujZUMC98KMlil4AkICL4`, SRC-31) is **primary**; copies in the group KB `Archive/`, the OneDrive tax archive (SRC-32) and the sister KBs are **secondary**. The full **byte-level duplicate register** is index v2 §7 (`14HbMMzhVcEZLj06Hcn5EXT0h3MgyhOWP`) and is not restated here: **nine byte-identical sets**, the largest being the entire 24-file incorporation folder duplicated between the group KB and the Commercial Properties KB `Raw/`, plus a **tenth found later through the Document Register** (`FH0000001`'s 241,121-byte annual report, identical to the group KB copy) — **found by a register query run after v2 was built, so it is deliberately not in v2 §7**, a provenance `AWT-0031` failed to state and Alex rightly challenged. It is the argument now made in `RA-15` for reading the register before walking Drive. **Where it stands — corrected 2026-09-19 after Alex's check on `AWT-0031`, which was right and which this row had overstated.** What had actually been retired at that point was **one** accounts duplicate (FP Members Accounts 2024, `1pXt93bGxM7joY0WQcl8T7R6UGpKh-8hv`) plus the **six** Government Gateway files out of the Commercial Properties KB incorporation folder (`RA-25`) — not the finance set as a whole. **All seven remaining byte-identical sets are Rachel's**, because they are finance documents: FW Members Accounts 2025 and FW Pages for Registrar 2025 (both duplicated *inside* the group KB `Archive/`), the 145 High Street East pack, the SSAS Loanback LA01801 **triplicate**, and the SSAS Legal Mortgage, Completion Statement and Receipted Invoice. Taken back on Minda's instruction 2026-09-19. They fold into the consolidation batches — the SSAS and loan items into Batch 3 — and each is handled the Batch 2 way: **check the destination first**, because “duplicate” does not mean “retire” until the archive has been checked. `AWT-0031` is re-scoped to the one genuinely structural question, which is not Rachel's to decide: whether the Commercial Properties KB should hold a second copy of the group's 24-file incorporation pack at all. **Rachel's remainder also includes:** the OneDrive `SSAS/` set (141 MB, duplicating the canonical Drive set) and the cross-cloud spreadsheet duplicates. **2026-09-19 — an eleventh set, and the most instructive one.** `FP0000004` registers the Properties FY2024 accounts against Drive id `1rw1ZJmpKxEEFvl8F7qS2G2r8bAY4_7Da`; the copy Rachel moved into the archive is `1M0AbOwowxbwu0v_YI30zTlJSST_mBUw4`. **Both are 1,095,647 bytes — the same document, two Drive files.** The Source-key dedup check did **not** see it; only reading all 197 titles did. Noted on the row, Source key left alone. A second oddity there: the file is named `FP0000003 - ...` though the row is `FP0000004`, and `FP0000003` is a Kensington mortgage offer — flagged, not corrected. **2026-09-19 — a twelfth, thirteenth and fourteenth, and a new class of duplicate.** Batch 2 found the Financial Archive **already held** the FP CT600 2025 and the CP CT600 2025, so three KB copies were duplicates of documents already in the single home: the FP group-KB copy (sha256-identical) and both CP KB copies. **This is the class index v2 could not see**, because §10 records that v2 never re-verified the archive itself. Every remaining batch now starts by checking the destination. **2026-09-19 — Batch 3 closed one set, dissolved two, and opened four.** **Closed:** the SSAS Loanback LA01801 triplicate, plus the SSAS Legal Mortgage, Completion Statement and Receipted Invoice pairs — one copy of each moved to the archive, five redundant copies retired and labelled, one sha256-verified. **Dissolved, and instructively:** the two FW sets (Members Accounts 2025, Pages for Registrar 2025) were recorded by index v2 as two copies each *inside* the group KB `Archive/`. They are **three** copies each, because the Financial Archive already held a primary at the same byte count that v2 never saw (§10 again). Nothing needed moving; the archive primaries were simply **unregistered**, and are now `FW0000005` and `FW0000006`. The two group-KB copies were left untouched — they sit in an `Archive/` folder, and tidying another employee's KB beyond taking a document out is outside `CHARTER.md` §3. **Opened — a fifteenth to eighteenth set, and the `FP0000004` pattern repeating exactly:** `FP0000017` (Board Resolution), `FP0000019` (Legal Mortgage executed), `FP0000020` (Loan Agreement executed) and `FP0000032` (RICS Valuation) are all registered against **different Drive ids** from the Landbay files Rachel just moved into the archive. Found only by reading every FP title; the Source-key check saw none of them. `FP0000101`'s registered copy is in **Collaboration Space** (`RA-21`), which Rachel may not write to. Registration of the Landbay pack is therefore **deliberately deferred**: minting would breach §4 and repointing would assert an identity not yet proved. This needs a byte-level comparison of each pair, which is `RA-15` work. **Full history in `open-issues-history.md` on Drive, verbatim.** **2026-09-20 — Batch 4 closed the incorporation sets, and `AWT-0031` closed with them.** The one structural question Rachel had left with Alex — whether the Commercial Properties KB should hold a second copy of the group's incorporation pack — was **answered by Minda instead**: move one, retire the CP duplicate. Twelve CP KB duplicates were retired to `Archive/`, labelled, never deleted, with sha256 controls on the two sets that carried no `(1)`-suffix tell — the Holdings incorporation pack (`287040330d…8ba9632c`, 641,237 bytes) and the Drylining change of name (`d83bb1ea88…a000ad45`, 20,885 bytes). **A correction belongs on this row.** Alex's `AWT-0031` objection was that both incorporation folders still carried the six Government Gateway files, which would have made the move a security problem. That was **true of index v2 §7's snapshot and not of live Drive** — those six had already left in Batch 1 (`RA-25`). It is the **same class of error Alex had correctly pulled Rachel up for** over the tenth set, and it points at the same conclusion `RA-15` argues: **index v2 is a dated snapshot** (§10 admits it never re-verified the archive), so a byte-level duplicate register is evidence of what was true when it was built, never of what is true now. Both of us reasoned from it; the register is what was stale. **Still open here:** the four Landbay pairs (`FP0000017`, `FP0000019`, `FP0000020`, `FP0000032`) and `FP0000004`'s pair, all needing a byte-level comparison before anything is repointed; `FP0000101` in Collaboration Space, which Rachel may not write to (`RA-21`); the OneDrive `SSAS/` set; and the cross-cloud spreadsheet duplicates. |

---

## `RA-19` — the per-batch consolidation narrative, as it stood at 2026-09-20

_The live `RA-19` was rewritten the same day to carry the method, the running totals across Batches 1–4, what remains, and the
standing exclusions. The per-batch accounts are **not** only here: Batches 1 and 2 are verbatim in `current-state-history.md`,
Batches 3 and 4 in the live `current-state.md`, and all four in the dated `change-log` entries in the group KB. This is the
fourth copy, kept because it is the one that records them **as issue-tracking rather than as narrative** — what each batch was
blocked on, what authority it needed, and what it changed about the method._

| ID | Opened | Status | Issue |
|---|---|---|---|
| RA-19 | 2026-09-19 | Open | **Consolidate the scattered financial documents into the designated main Financial Archive.** The home is decided (`RA-10`, folder `1BVk_RfuJ3rBRujZUMC98KMlil4AkICL4`); all three `RA-20` authorities are granted; the group-policy conflict is settled by the owner's precedence ruling (`RA-30`). **Method (`CHARTER.md` §3, fixed):** copy in → byte-verify → register → retire the source, and where both ends are on Drive a **move** is used instead, because it preserves the file id. Rachel deletes nothing. **Progress:** **Batch 1 DONE 2026-09-19** — the dated FY2024 accounts (Properties, Holdings, Waste) and the dated FY2023 pair (Construction) moved into their company `Annual Accounts` subfolders, ids and byte sizes unchanged, verified, one byte-identical duplicate retired. Taken first because those five were the **only copies in existence**. **Batch 2 DONE 2026-09-19** — and it was mostly not a move. Checking the destination first (which index v2 §10 admits it never did) showed the Financial Archive **already held** the FP and CP CT600 2025s, so of Batch 2's four files **one moved and three were retired**: FC's CT600 + computation YE 29 Apr 2024 moved into `Fishbone Construction Ltd > Annual Accounts`, id and 1,011,120 bytes unchanged; the FP group-KB copy and both CP KB copies were sha256-checked against the archive primaries and retired to `Archive/`, labelled, never deleted. Registered **`FC0000016`** (new) and **`FP0000142`** (the archive primary, which had never been registered at all); `FM0000006` already covered the CP CT600 so no number was minted, only its Location corrected. **Batch 3 DONE 2026-09-19** — and it began blocked. The archive had **no folder for a loan document**: every company folder held only `Annual Accounts` plus tax-year folders, and creating a folder type is **reshaping**, which goes to Minda (`RA-20`). She ruled **one `Loans` folder at the archive root** (`1aAKayaQxQTzvWuB-DKhfzhlyOVdjHXUn`), subdivided by facility, plus a **seventh top-level `Fishbone SSAS` folder** (`1xmngPPoM5bf1JkU6w_7vD_1Yiwvf7pEk`) for a scheme the register already had an `FS` prefix for. **Moved 21 files, 12,020,057 bytes, every id and byte size unchanged:** Landbay 70092357 (11, the two personal guarantees included on Minda's instruction), SSAS Loanback LA01801 (8), MotoNovo HP (1), LendInvest 60006183 (1). **Five redundant copies retired** to `Archive/`, labelled, never deleted — four `(1)`-suffixed download artefacts, and the Commercial Properties copy of the Loanback agreement **sha256-verified identical** first. **Registration went the other way from every batch so far:** all eight SSAS Loanback documents were **already registered** as `FS0000001`–`FS0000008`, so **no number was minted** (§4) and only Location was corrected. The Landbay pack is moved but **not registered** — four of its documents already have FP rows against different Drive ids (`RA-11`). One mis-filing by Rachel, caught and fixed: the DocuSign zip is **LendInvest 60006183**, not Landbay. `FS0000009`–`FS0000013` left in the SSAS KB, outside the named scope. **Remaining:** Batch 4 (certificates, credentials excluded) and Batch 5 (bank-statement series, folder convention to agree) are clear to run — but each must now begin by checking what the archive already holds. **Never in any batch:** pension records for named individuals, payroll reports, tenant identity documents, and anything still unidentified (`RA-28`). **Sequencing changed 2026-09-19:** reconcile against the Document Register **first** (`RA-15`) — it points at KB `Outputs/` folders index v2 never opened, so it is a faster route to what is still unmapped than walking Drive. **Full dated history of this issue — the original scope statement, the blocked period, the authority questions and the move-list draft — is in `open-issues-history.md` on Drive, verbatim.** **Batch 4 DONE 2026-09-20** — the **incorporation and registration set**, and like Batch 3 it needed a home before it could start: the archive root held loose statutory files and no folder that would take a certificate of incorporation or a VAT registration. Minda ruled a **`Registration & Tax IDs` folder at the archive root** (`16rFY1xQjWvnTX1LpE3_Lst91duj8K7Gc`), seven subfolders — the six companies plus `Group`. **Moved 18 files, 2,986,136 bytes, every id and byte size unchanged**, and the archive root now holds **nine folders and zero loose files**. **Twelve Commercial Properties KB duplicates retired** to `Archive/`, labelled, never deleted, two of them **sha256-verified first** because they carried no `(1)`-suffix tell. **Eleven numbers minted, seven Locations corrected** — register at **221 rows** — including two that only reading could settle: `FH0000022` is a composite incorporation **pack**, not a certificate, and `FA0000004` is a genuinely different document from `FA0000001`, because company 11259604 was renamed twice. **The order-of-operations deviation recorded against Batch 3 repeated:** the retirements ran **before** registration again, because the batch paused between the two for Minda's go-ahead on the numbering. Nothing was at risk — retirement depended on the primary being byte- or hash-verified, never on its having a number — but the fixed method has now been written one way and run another twice, so either §3 should say so or the pause should come earlier. **Remaining: Batch 5 only** — the bank-statement series, still needing a folder convention from Minda, though `Loans` and `Registration & Tax IDs` now suggest its shape. **Never in any batch** is unchanged. |

---

## `RA-15` — the registration-and-sampling paragraph, as written 2026-09-19

_Moved out of the live row on 2026-09-20. Its rule is **not** retired with it: read the whole sheet, pass explicit `columns`,
confirm `isSampled: false`, and check the high-water mark **at the moment of assignment**. That is carried in the live `RA-15`,
in `current-state.md`'s standing-rule header, and has been applied at every registration since. What moved is the account of the
first eleven registrations and of how the sampling trap was found._

> **2026-09-19 — first registrations done, and a sampling trap found.** The eleven consolidated documents are now accounted for: nine new IDs minted (`FH0000021`, `FM0000009`, `FP0000141`, `FA0000002`, `FC0000013`, `FG0000001`, `FW0000004`, `FC0000014`, `FC0000015`) and two already registered (`FH0000003`, `FP0000004`) correctly given no new number. **The register's default read is SAMPLED** — it returned `isSampled: true` with **83 of 197 rows**, being simply the first in ascending order, which would have produced a next-free FP number of `FP0000043` when the true maximum is `FP0000140`. Always pass explicit `columns` and confirm `isSampled: false` before trusting any high-water mark.

---

## `RA-28` — the unidentified-files row as it stood at 2026-09-20

_The live row was rewritten the same day, and one thing changed in the rewrite that is worth being explicit about: the heading
below says **eleven** files, and its own list totals **twelve**. The live row now says twelve and says that the heading was wrong
from the day the row was opened. The error is preserved here rather than tidied out of existence, because a count that nobody
checked for two days is exactly the kind of thing this file exists to keep._

| ID | Opened | Status | Issue |
|---|---|---|---|
| RA-28 | 2026-09-19 | Open | **Eleven files in finance folders cannot be identified from their names.** Index v2 will not guess what a document is. In the Commercial Properties KB `Raw/`: `Xerox Scan 20260907 122513.pdf` (**13,333,897 bytes** — the largest single file the sweep found, almost certainly a multi-document scan batch), `companies_house_document.pdf` (611,971), `ufm5_ACKSAP_APPL.pdf` (76,764) and three UUID-named PDFs (45,494 / 46,654 / 50,037). In both incorporation folders: `download.pdf` (123,650), `Document_2021-04-20_100425.pdf` (645,590), `Document_2021-04-07_111051.pdf` (1,211,987) and two photographs. And in the Amfa KB `Raw/`: `2026-01-27_BP scheme.pdf` (46,605). None has been opened. They are held at **folder level** in the index and excluded from every consolidation batch, because filing a document whose identity is unknown is how the current mess was made (see `RA-18`: a folder name is not a classification). Next step is a short classification pass — open, identify, name properly, then index at file level. **Correction 2026-09-19:** this row has been described elsewhere as gating Batch 2. It does not, and never did — unidentified files are excluded from **every** batch as a standing rule, and Batch 2's scope was fully defined without them. **2026-09-20 — Batch 4 did not reduce this list, and that is worth stating plainly rather than leaving to inference.** Everything Batch 4 identified, it identified from a **title or a filename reference**; it opened none of the files named above. The five items listed here as sitting *in both incorporation folders* — `download.pdf`, the two `Document_2021-04-…` PDFs and the two photographs — are **exactly the five still there** now that Batch 4 has taken the statutory contents out; the sixth item now in each folder is the **Shakerbone** certificate, ruled out of scope by Minda and so not Rachel's to identify. The six in the Commercial Properties KB `Raw/` and the one in the Amfa KB `Raw/` were never in Batch 4's scope and are untouched. **A count error on this row, found while checking that and left visible rather than renumbered:** the heading says **eleven** but its own list totals **twelve** items. Twelve is the number still unidentified. The heading is wrong and has been since the row was opened; the discrepancy is the evidence, so it is corrected here in the narrative instead of being quietly overwritten. The 13,333,897-byte Xerox scan remains the likeliest multi-document batch and the one worth opening first. |

---

## Moved 2026-09-20, afternoon

_A second move into this file the same day. The morning's move was driven by Batch 4; this one by Batch 5, the AGGA sandbox work and the day's governance changes. Same rule: **the whole cell, verbatim**, never a chosen extract._

### `RA-19` — as it stood after Batch 5 started

_Rewritten as a current-position row carrying running totals across all five batches. The per-batch narrative was by then in five other places; this is the sixth and the one that records it as issue-tracking — what each batch was blocked on and what it changed about the method._

| ID | Opened | Status | Issue |
|---|---|---|---|
| RA-19 | 2026-09-19 | Open | **Consolidate the scattered financial documents into the designated main Financial Archive.** The home is decided (`RA-10`, folder `1BVk_RfuJ3rBRujZUMC98KMlil4AkICL4`); all three `RA-20` authorities are granted; the group-policy conflict is settled by the owner's precedence ruling (`RA-30`). **Method (`CHARTER.md` §3, fixed):** copy in → byte-verify → register → retire the source; where both ends are on Drive a **move** is used instead, because it preserves the file id. Rachel deletes nothing. **Every batch now begins by checking what the archive already holds** — Batch 2's lesson, and index v2 §10 admits v2 never did it. **Position at 2026-09-20: Batches 1–4 DONE, Batch 5 remaining.** Across the four: **44 files moved**, every id and byte size unchanged; **20 redundant copies retired** to `Archive/`, labelled, never deleted, with sha256 verification wherever no `(1)`-suffix tell existed; **23 numbers minted and 16 Locations corrected**, taking the register from 197 rows to **221**. Three archive folder types were authorised by Minda along the way — `Loans` and `Fishbone SSAS` in Batch 3, `Registration & Tax IDs` in Batch 4 — because creating one is **reshaping**, which goes to her and not to Rachel. The archive root now holds **nine folders and zero loose files**. **Batch 5** is the **bank-statement series** and still needs a folder convention from Minda, though those two rulings now suggest its shape. **Never in any batch:** pension records for named individuals, payroll reports, tenant identity documents, and anything still unidentified (`RA-28`). **Sequencing:** reconcile against the Document Register **first** (`RA-15`) — it points at KB `Outputs/` folders index v2 never opened. **One method deviation, now recorded twice:** in Batches 3 and 4 the retirements ran **before** registration rather than after. Nothing was at risk — retirement depended on the primary being byte- or hash-verified, never on its having a number — but §3 has now been written one way and run another twice, so either §3 should say so or the pause for Minda's go-ahead should come earlier. **Where the per-batch narratives live.** They are not summarised away: Batches 1 and 2 are verbatim in `current-state-history.md`, Batches 3 and 4 in the live `current-state.md`, all four in the dated `change-log` entries, and this row as it stood before the 2026-09-20 rewrite is verbatim in `open-issues-history-2026-09.md`. The original scope statement, the blocked period, the authority questions and the move-list draft remain in `open-issues-history.md`. **Batch 5 STARTED 2026-09-20, Construction done, four companies to go.** The **bank-statement series**, and the destination check changed it again: the archive already held **eighteen bank-statement folders under eleven different names**, plus three places holding loose statements and no folder. Minda ruled **folders inside each company, all named the same**; the approved shape is **`<Company>/Bank Statements/<account>/<format>`**, each account keyed to its **account number**, with **all years of an account in one continuous series**. That last choice is deliberate — chopping a series by tax year is what hid `RA-7` and `RA-8` — and it paid off immediately: Construction's BBL series now shows its stop at May 2024 at a glance, making `RA-8` **visible rather than asserted**. Waste's company-level `Bank` folder was **renamed, not duplicated**. **Construction: 14 folders and 32 files moved**, ids and byte sizes unchanged; **two duplicates sha256-verified and retired**, the same statement having been filed under **two different tax years**. **Method changed mid-batch on Minda's instruction** — folder moves where material was already grouped, per-file only for loose files, roughly 20× fewer operations. **A second source found through `Raw/`, and it widens this issue:** the company KBs hold **live 2026 statements more recent than anything in the archive** — Properties' accounts `84311663` and `24643100` running March–August 2026, and two Commercial Starling series. Index v2 said statements were being captured into the KBs rather than the archive; this is that, concretely, and it means Batch 5 has two sources, not one. |

### `RA-24` — as it stood when the AGGA offset arrived

_Rewritten the same day to carry the sandbox assessment's headline findings. The full analysis is in `Sandbox/2026-09-20_sandbox-01_intercompany-offset_AGGA.md`, which is a **working paper, not a register document**._

| ID | Opened | Status | Issue |
|---|---|---|---|
| RA-24 | 2026-09-19 | Open | **Intercompany loans and unpaid corporation tax — with the headline claim corrected.** The source owner-note (Commercial Properties KB, 2026-09-07, `1BblZb0doDVbcIrgiV5ouomBEstk6NZiv`) says there are **no written intercompany loan agreements**. Checking the Document Register shows that is **too broad**: the Holdings→Properties chain is **fully papered** (`FH0000008` request, `FH0000009` board minute, `FH0000012` letter of variation, `FH0000013` acceptance, `FH0000016` confirmation of receipt) and **two loan agreements exist** — `FH0000010` (£283,000 at 3.8%, 2019-05-01) and `FH0000011` (£20,000 at 6%, 2021-10-15). **What stands:** the *other* intercompany balances may still be undocumented, which bears on related-party disclosure; and **corporation tax is unpaid** — Commercial's CT600 to 31 Oct 2024 records £1,484.47 unpaid at signing, and Waste carries £3,042.33 of business rates with a 21/08/26 liability order. **2026-09-19 — corroborated from the register itself:** `FM0000006`'s description records box 595 (tax already paid) nil and box 600 outstanding the full £1,484.47 against a **1 August 2025 due date**, so it was unpaid when signed on 30 April 2026. It also records **box 326 declaring four associated companies**, which bears on `RA-9`. **For Minda and RMT, not Rachel** — §3 forbids her committing the company. She has not contacted HMRC or the accountant. **Full history in `open-issues-history.md` on Drive, verbatim.** **2026-09-20 — this issue is now live adviser work, in the sandbox.** AGGA (Alexey Glukhov) has proposed a **four-part intercompany offset** effective 29/4/26, consolidating Commercial Properties' whole intercompany debt at Holdings and asking for confirmation by 30/9. Assessed under `CHARTER.md` §6 — see `Sandbox/2026-09-20_sandbox-01_intercompany-offset_AGGA.md`. **The memo's arithmetic ties**, independently re-derived. **The finding that matters: only ONE of the five balances is confirmed on both sides.** Two are one-sided on the adviser's own basis column — £97,319 and £103,251, both owed by Commercial Properties — and together they are **£200,570, the entire sum being moved**, evidenced by nobody but the counterparties' own notes. The memo's open items scrutinise a **£2,100** discrepancy and say nothing about the £200,570. **The £2,100 is probably not an error at all** but a 29/30 April timing difference, which makes it a symptom of the year-end mismatch rather than a reconciling item — and both remedies the memo offers would misstate something. **Lender consent is unaddressed across all six pages**, though Properties' Landbay and LendInvest facilities carry personal guarantees and Commercial's SSAS loanback carries regulatory conditions. Recommendation: **do not confirm by 30/9**; evidence the £200,570 first, settle the year end, get the lender position. Reply drafted and staged for Minda to send. |
